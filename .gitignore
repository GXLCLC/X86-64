# ==============================================================================
# .gitignore —— Git 忽略规则
# ------------------------------------------------------------------------------
# 本仓库只保存"编译配置与脚本"，不保存 OpenWrt 源码和编译产物。
# 以下规则保证：即使你把 LEDE 源码克隆到本仓库目录内本地编译，
# 庞大的源码/中间产物/固件也不会被误提交到 GitHub。
# ==============================================================================

# ------------------------------------------------------------------------------
# 一、OpenWrt/LEDE 源码与本地编译产物
# CI 中源码克隆在 openwrt/ 子目录；本地编译若也放在该目录会被整体忽略
# ------------------------------------------------------------------------------
openwrt/
lede/

# 本地编译时在源码目录内生成的目录/文件（若源码放在仓库根目录也能忽略）
bin/
build_dir/
staging_dir/
staging_dir_host/
dl/
tmp/
logs/
feeds/
package/feeds/
config/conf
.config
.config.old
.config.orig
feeds.conf
version.mk
key-build*
*.manifest

# ------------------------------------------------------------------------------
# 二、固件镜像与发布中间文件
# 只发布 .img.gz；其它格式本就不生成，这里一并忽略防止误传
# ------------------------------------------------------------------------------
*.img
*.img.gz
*.vmdk
*.vdi
*.vhdx
*.iso
*.qcow2
firmware/
plugins.txt
sha256sums.txt

# ------------------------------------------------------------------------------
# 三、补丁临时文件
# ------------------------------------------------------------------------------
patches/*.orig
patches/*.rej
*.rej
*.orig

# ------------------------------------------------------------------------------
# 四、操作系统与编辑器临时文件
# ------------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.idea/
.vscode/
